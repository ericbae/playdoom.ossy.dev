{
  "name": "playdoom-ossy-dev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 8788",
    "deploy": "wrangler deploy",
    "video:render": "node video/render-video.mjs"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "wrangler": "^4.90.0"
  }
}
